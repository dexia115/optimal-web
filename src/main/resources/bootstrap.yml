CONFIG_SERVER_URL: http://127.0.0.1:8888

server:
  port: 8090
  tomcat:
    uri-encoding: UTF-8
spring:
  application:
    name: optimal-web
  cloud:
    config:
      enabled: false
      uri: ${CONFIG_SERVER_URL}